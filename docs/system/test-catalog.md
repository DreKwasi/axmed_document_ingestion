# Test Catalog

> Purpose: inventory meaningful automated tests and the behavior each protects.
> Status: Slices 1-5, 8, 9 delivered — comprehensive backend, Vue, API-background-task, E2E, and PII audit checks in place.
> Update in the same change whenever tests are added, removed, renamed, or materially repurposed.
> Owner persona: quality engineer.
> Related: `docs/system/testing.md`, `docs/system/false-confidence-audits.md`.
> Search terms: test catalog, coverage, journey, regression, owner, batch, pii.
> Each entry describes what could break, not merely the test filename.

| Test / suite | Layer | Protects | Known limitations | Worksheet |
| --- | --- | --- | --- | --- |
| `backend/tests/test_json_extraction.py` | API/integration | independent nested, flat, and mixed JSON extraction; high-confidence unmapped facts; JSONPath/value validation retries; partial fact retention; graceful zero-product failure; explicit re-extraction; and removed mapping endpoint | recorded and scripted extractors cover contract behavior; live model quality needs corpus evaluation | json-extraction-without-schema-reuse |
| `backend/tests/test_entrypoint.py` | unit | the simple backend-root `main:app` Uvicorn entry point remains importable and exposes the configured FastAPI application | it does not replace API or lifespan integration coverage | root-backend-entrypoint |
| `backend/tests/test_migrations.py` | integration | fresh startup, pre-Alembic baselining, repair of a database stamped past a missing review-learning table, normalized line-item backfill/precision, derived-price validation status, field-value review metadata backfill, mandatory-human-review migration, zero-product failure backfill without rewriting completed reviews, and removal of retired route/HS/ATC line-item fields | intentionally damaged historical shape is simulated; production backups still precede migration operations | DI-01, frontend-home-review, confidence-v9, supply-and-code-semantics |
| `backend/tests/test_evaluations.py` | API/integration | SQLite-backed evaluation case/run/result, source-grounding scores, live-PDF, live-email, and OCR-anchor case selection/persistence, and rubric scores; `backend/bin/run-evals` executes this suite before a persisted recorded run | live provider responses are mocked in automated tests; grow to MVE set | json-extraction-without-schema-reuse, DI-12 |
| `frontend/src/App.spec.ts` | component | single-title Home shell with the main-page ingestion action, clean uploaded sources table, direct clickable filename download link subtext without separate Download file label, immediate separate OCR-assisted/direct-vision source results, direct candidate review without comparison interstitial, staying on Home after image upload, reading switcher in detail header, tooltip dismissal on outside clicks, Escape key, and drawer opening, extraction-confidence and mapping-confidence columns, explicit no-issues mapping results, numeric per-product mapping-issue counts, section-level product-detail issue placement, document-level HS-code, shipping transit duration, and payment terms display, full dosage-form labels, correction state, every product-detail correction category, structured review decision dialog, non-redundant source schema / unstructured document display, and multi-file upload states | browser coverage is intentionally deferred; evaluation API remains backend-only | direct-image-peer-sources, shipping-transit-duration, clickable-filename-subtext |
| `frontend/src/exportCsv.spec.ts` | unit | Home CSV export excludes in-progress sources; emits one user-facing row per extracted product or terminal productless source; consolidates currency, packaging, issues, reviews, and active-ingredient lists; and explains extraction confidence without internal IDs, statuses, record types, or source-fact rows | issue and review details are semicolon-delimited when several values apply to one product row | extraction-mapping-confidence, user-facing-csv-export |
| `backend/tests/test_commercial_review.py` | API/integration | source-specific price UOM preservation, no generic-UOM fallback, deterministic unit-price derivation or repetition of an explicit unit quote with formula/validation status and no extraction-confidence band, source-file retrieval/deletion, commercial validation, supported product-detail and commercial-terms transit duration field correction with reviewer notes and leaf review synchronization, rejection of unknown/derived fields, before/after audit tracking, correction evidence/learning job, structured rejection reasons, mandatory pending-review queue, idempotency, and stale-review conflict | richer tier/discount fixtures arrive with their source formats; background learning runs after PII redaction | DI-02, confidence-v9, normalized-unit-price-fallback, source-deletion, shipping-transit-duration |
| `backend/tests/test_commercial_rules.py` | unit | date validity, percentage bounds, non-overlapping tiers, and compatible quoted-quantity/MOQ checks at the deterministic validation seam | only rules with a canonical source field are added; source aliases stay out of this suite | DI-02 |
| `frontend/e2e/review.spec.ts` | end-to-end | isolated API/web startup, row-click product drawer inspection, in-place price correction/derivation, review modal trigger, explicit approval, downloaded user-facing product CSV contract, exactly one review request when opening a completed image result, and terminal failed image attempts showing mapping confidence as not applicable | JSON uses a recorded fixture; image result states use browser-level API contract stubs because live OCR is separately smoke-tested | DI-02, frontend-overhaul, source-detail-review-endpoint, failed-mapping-confidence-state, user-facing-csv-export |
| `backend/tests/test_processing_events.py` | API/unit | persisted safe event metadata, contact redaction, and invalid SSE reconnect cursors | browser replay has a component seam; richer document stages arrive with parser slices | DI-03 |
| `backend/tests/test_settings.py` | unit | relative operational database and upload paths resolve from the backend configuration directory | non-SQLite deployments retain their supplied database URL | api-background-processing |
| `backend/tests/test_email_parser.py` | API/unit | MIME plain-text selection, greeting/signature PII minimization, contact redaction, non-rendered HTML, safe email intake state, durable extraction lifecycle with verbose preparation/semantic/normalization events, Gemini context, commercially validated quotation, and migration-backed storage | live consumer coverage remains | DI-04, home-source-table |
| `backend/tests/test_email_reconciliation.py` | unit | explicit source-email price UOM wins over a model-normalized UOM only when item identity and amount agree | does not infer UOMs from packaging or unmatched source text | DI-04, DI-12 |
| `backend/tests/test_pdf_parser.py` | API/unit | LiteParse-only PDF signature safety, in-process corrupt/malformed PDF fail-fast safety, clean native-text/table reading order, deterministic page planning, layout-aware primary semantic context, narrative-page semantic context, page-level quality, OCR avoidance, safe parsed-artifact persistence, durable extraction lifecycle with verbose preparation/semantic/normalization events, source summary fields, and reviewable output | In-process native Rust parsing eliminates external CLI subprocesses; Railpack builds as a pure Python service; corpus evaluation remains | DI-05, DI-13, semantic-pdf-enrichment, upgrade-liteparse-v2 |
| `backend/tests/test_confidence.py` | unit | explicit weighted source-recovery scoring for machine-readable, native-text, and OCR sources; independent canonical mapping scores; conflict-to-section mapping issues; and deduplication of actionable issues | weights are initial policy values and require calibration against expanded labeled data | extraction-mapping-confidence |
| `backend/tests/test_ocr_contract.py` | unit | versioned OCR provider response, original page identity, confidence, coordinates, dimensions, DPI, malformed-response rejection, authenticated bounded HTTP request, and idempotency key | live service is separately smoke-tested and benchmarked; model quality still needs approved ground truth | DI-06 |
| `backend/tests/test_image_parser.py` | API/unit | supplied low-resolution fax and glare-partial OCR fixtures, PNG/JPEG signature and dimensions, independent OCR-job creation, and safe awaiting-configuration lifecycle | live OCR evidence and semantic follow-on remain | DI-06 |
| `backend/tests/test_document_uploads.py` | API/integration | one document endpoint accepts single or multiple files, schedules each PDF, isolates per-file failures, logs unexpected upload exceptions with safe request correlation, preserves CORS on handled 500 responses, and does not expose retired batch/evaluation/diagnostics/review-queue routes | live OCR in multi-file upload deferred to Slice 6 deployment | backend-structure-simplification, api-background-processing, upload-exception-logging |
| `frontend/e2e/batch.spec.ts` | end-to-end | multiple files use the single document-ingest action and each source appears independently in the table | JSON multi-file journey; E2E files run serially because they share one isolated server/database | DI-08, backend-structure-simplification |
| `backend/tests/test_pii_audit.py` | unit/integration | deterministic phone/email scrubbing, recursive model context redaction, and event/diagnostic leakage protection | Presidio integration when advanced NER is configured | DI-09 |
| `backend/tests/test_langchain_gemini.py` | integration/unit | LangChain + Gemini structured output across email, PDF, OCR, and JSON semantic facts; images persist peer OCR-assisted text and OCR-gated vision results with no automatic winner; below-gate sources invoke no semantic model and fail both attempts safely; chronology correction supersession, complete dosage-form preservation, packaging quantity parsing, active-moiety normalization, and narrative enrichment without table-value overwrite | simulated model responses in CI; live API key runs against Google Gemini | json-extraction-without-schema-reuse, DI-11, DI-14, semantic-pdf-enrichment, ocr-multimodal-semantic-extraction |
