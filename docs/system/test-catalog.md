# Test Catalog

> Purpose: inventory meaningful automated tests and the behavior each protects.
> Status: Slices 1-5, 8, 9 delivered — comprehensive backend, Vue, worker, E2E, and PII audit checks in place.
> Update in the same change whenever tests are added, removed, renamed, or materially repurposed.
> Owner persona: quality engineer.
> Related: `docs/system/testing.md`, `docs/system/false-confidence-audits.md`.
> Search terms: test catalog, coverage, journey, regression, owner, batch, pii.
> Each entry describes what could break, not merely the test filename.

| Test / suite | Layer | Protects | Known limitations | Worksheet |
| --- | --- | --- | --- | --- |
| `backend/tests/test_schema_learning.py` | API/integration | cold mapping proposal, human confirmation, trusted warm cache, schema-version isolation, reviewable schema conflict, receipt identity, malformed mapping failure, JSON validation | recorded mapper only; live model comes later | DI-01 |
| `backend/tests/test_canonical_nulls.py` | unit | a missing required source value is preserved as `null` and produces an explicit review issue | field criticality is declared by the mapping fixture; more corpus cases needed | DI-01 |
| `backend/tests/test_migrations.py` | integration | fresh startup, pre-Alembic baselining, repair of a database stamped past a missing review-learning table, normalized line-item backfill/precision, derived-price validation status, field-value review metadata backfill, and the mandatory-human-review migration | intentionally damaged historical shape is simulated; production backups still precede migration operations | DI-01, frontend-home-review, confidence-v9 |
| `backend/tests/test_evaluations.py` | API/integration | SQLite-backed evaluation case/run/result, cold/warm mapping contract, live-PDF, live-email, and OCR-anchor case selection/persistence, actual zero warm tokens/cost, and rubric scores; `backend/bin/run-evals` executes this suite before a persisted recorded run | live provider responses are mocked in automated tests; grow to MVE set | DI-01, DI-12 |
| `frontend/src/App.spec.ts` | component | single-title Home shell with the main-page ingestion action, clean uploaded sources table, source naming, source confidence, explicitly named Review issues, product detail drawer confidence summary, correction state, structured review decision dialog, and batch upload states | browser coverage exercises the primary upload journeys; evaluation API remains backend-only | home-source-table, ingest-source-action, frontend-overhaul, confidence-v9 |
| `backend/tests/test_commercial_review.py` | API/integration | source-specific price UOM preservation, no generic-UOM fallback, deterministic unit-price derivation with persisted formula/validation status and no extraction-confidence band, source-file retrieval, commercial validation, arbitrary canonical field correction with reviewer notes, before/after audit tracking, correction evidence/learning job, structured rejection reasons, mandatory pending-review queue, idempotency, and stale-review conflict | richer tier/discount fixtures arrive with their source formats; queued learning executes after the PII-safe worker exists | DI-02, confidence-v9 |
| `backend/tests/test_commercial_rules.py` | unit | date validity, percentage bounds, non-overlapping tiers, and compatible quoted-quantity/MOQ checks at the deterministic validation seam | only rules with a canonical source field are added; source aliases stay out of this suite | DI-02 |
| `frontend/e2e/review.spec.ts` | end-to-end | isolated API/web startup, mapping confirmation, row-click product drawer inspection, in-place price correction/derivation, review modal trigger, and explicit approval | one JSON fixture; batch and non-JSON journeys arrive with their parser slices | DI-02, frontend-overhaul |
| `backend/tests/test_processing_events.py` | API/unit | persisted safe event metadata, contact redaction, and invalid SSE reconnect cursors | browser replay has a component seam; richer document stages arrive with parser slices | DI-03 |
| `backend/tests/test_settings.py` | unit | relative operational database, Huey queue, and upload paths resolve from the backend configuration directory | non-SQLite deployments retain their supplied database URL | worker-path-consistency |
| `backend/tests/test_learning_worker.py` | integration | resolver request redaction, durable invocation/preference result, trusted-mapping demotion, terminal failure state, and safe diagnostics | configured resolver is mocked; credentials/provider adapters remain an operator configuration choice | DI-03 |
| `backend/tests/test_email_parser.py` | API/unit | MIME plain-text selection, greeting/signature PII minimization, contact redaction, non-rendered HTML, safe email intake state, durable extraction lifecycle with verbose preparation/semantic/normalization events, resolver context, commercially validated quotation, and migration-backed storage | malformed resolver and live consumer coverage remain | DI-04, home-source-table |
| `backend/tests/test_email_reconciliation.py` | unit | explicit source-email price UOM wins over a model-normalized UOM only when item identity and amount agree | does not infer UOMs from packaging or unmatched source text | DI-04, DI-12 |
| `backend/tests/test_pdf_parser.py` | API/unit | LiteParse-only PDF signature safety, clean native-text/table reading order, deterministic page planning, layout-aware primary semantic context, narrative-page semantic context, page-level quality, OCR avoidance, safe parsed-artifact persistence, durable extraction lifecycle with verbose preparation/semantic/normalization events, source summary fields, and reviewable output | corpus evaluation remains | DI-05, DI-13, semantic-pdf-enrichment |
| `backend/tests/test_confidence.py` | unit | per-field decisions from source clarity, association certainty, independent arithmetic/pack validation, and conflict precedence; absent-value neutrality; derived-value exclusion; and `pending_review` routing without averaging or critical-field gates | OCR quality cutoffs remain initial policy values and require calibration against expanded labeled field-level evaluation data | confidence-v9 |
| `backend/tests/test_ocr_contract.py` | unit | versioned OCR provider response, original page identity, confidence, coordinates, dimensions, DPI, malformed-response rejection, authenticated bounded HTTP request, and idempotency key | live service is separately smoke-tested and benchmarked; model quality still needs approved ground truth | DI-06 |
| `backend/tests/test_image_parser.py` | API/unit | supplied low-resolution fax and glare-partial OCR fixtures, PNG/JPEG signature and dimensions, independent OCR-job creation, and safe awaiting-configuration lifecycle | live OCR evidence and semantic follow-on remain | DI-06 |
| `backend/tests/test_batch_processing.py` | API/integration | multi-file batch creation, derived aggregate progress calculation, batch listing/retrieval, and failure isolation (corrupt file fails while valid siblings succeed) | live OCR in batches deferred to Slice 6 deployment | DI-08 |
| `frontend/e2e/batch.spec.ts` | end-to-end | multi-file upload in browser, aggregate batch indicator, and independent document processing | JSON multi-file journey | DI-08 |
| `backend/tests/test_pii_audit.py` | unit/integration | deterministic phone/email scrubbing, recursive model context redaction, and event/diagnostic leakage protection | Presidio integration when advanced NER is configured | DI-09 |
| `backend/tests/test_langchain_gemini.py` | integration/unit | LangChain + Gemini 3.1 Flash Lite structured output across email, PDF, OCR, and novel JSON mapping, chronology correction supersession, core dosage-form normalization, explicit packaging quantity parsing, active-moiety normalization, semantic narrative enrichment without table-value overwrite, and zero-cost schema cache bypass | simulated model responses in CI; live API key runs against Google Gemini | DI-10, DI-11, DI-14, semantic-pdf-enrichment |
