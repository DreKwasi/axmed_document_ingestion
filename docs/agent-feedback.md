# Agent Feedback Log

> Purpose: concise operational feedback that improves this repository’s agent workflow.
> Add one entry at the end of each substantial session, even if no issue was found.
> Record the signal, impact, and proposed durable improvement; do not include chain-of-thought.
> Periodic sweeps cluster repeated feedback and update workflows, tools, or docs.
> Commit entries with their associated work so feedback remains historically connected.
> Keep this log actionable and remove superseded guidance only through a documented sweep.

| 2026-09-07 | confidence-v7 | Treating an absent commercial value as low confidence made the UI claim that a missing field was an untrustworthy extraction. | Assess confidence only for recovered source facts; model commercial availability and review routing separately. | applied |
| 2026-09-07 | supply-and-code-semantics | Copying a shipment-level customs code to individual products made an ambiguous logistics fact look like product regulatory data. | Keep HS codes in document commercial terms; remove retired fields across normalized projections, snapshots, review values, and evidence together. | applied |
| 2026-09-07 | root-backend-entrypoint | A deeply nested Uvicorn import made the local startup command harder to remember and encouraged manually supervised processes without reload. | Provide a thin `backend/main.py` entry point and use it consistently in local and E2E launch commands. | applied |
| 2026-09-07 | confidence-v8 | A confidence engine that classified only a subset of fields left other persisted source facts with generic legacy labels and no auditable explanation. | Apply the same factorized confidence decision to every extracted source leaf without an unclassified fallback. | applied |
| 2026-09-07 | confidence-policy-no-critical-fields | Carrying forward a fixed required-field list contradicted the source-fact confidence model and created review issues for values a supplier simply did not provide. | Never infer criticality from a universal field list; route review only from observed extraction evidence, explicit failures, conflicts, or validation outcomes. | applied |
| 2026-09-07 | prd-v9-mandatory-review | A supplier-specific regex proposal for PDF metadata would have passed one fixture while failing different layouts, languages, and supplier phrasing. | Keep document meaning at the semantic-extraction boundary: give the LLM generic cross-section instructions, retain parsed source context/evidence, and add a regression check for the resulting canonical fields. | applied |
| 2026-09-07 | semantic-pdf-enrichment | Removing PDF geometry to save tokens caused the model to misassociate table quantities and prices, even though it recovered narrative shelf-life terms. | Keep layout-aware context for the table pass; reduce context only in a bounded, source-keyed narrative enrichment pass that cannot replace structured facts. | applied |
| 2026-09-07 | native-pdf-confidence-provenance | Missing leaf-level evidence in a clean native PDF expanded into repeated Low-confidence chips despite successful extraction. | Treat clean native PDF content without a saved leaf location as Medium and reserve review exceptions for Low-confidence or deterministic failures. | applied |
| 2026-09-07 | extraction-status-indicator | Displaying an ongoing extraction history card with all past states (1, 2, 3) simultaneously after completion caused visual clutter and confusion. | Display a single active progress state only while extraction is in flight, updating dynamically per event and completely dismissing once extraction completes. | applied |
| 2026-09-07 | frontend-overhaul | Rendering metadata (confidence, format, review status) in identical rounded pills alongside action buttons confused users and tests on interactive affordances. | Place informational metadata inline with document descriptors using text and status indicators, reserving high-contrast pills and borders strictly for interactive buttons. | applied |
| 2026-09-06 | frontend-home-review | A UI showed a null MOQ while the source quantity was correctly persisted as `quoted_quantity`; JSON aggregate reads obscured the distinction. | Keep active extracted line-item fields relational and name UI fields after their canonical quantity semantics; preserve JSON only as a revision snapshot. | applied |
| 2026-09-06 | field-review-persistence | Normalized scalar tables alone still made it possible to lose field-level review state and confidence outside the JSON evidence array. | Treat extracted values as first-class rows with canonical paths, provenance, confidence, and explicit human-review transitions; backfill them in the same migration chain. | applied |
| 2026-09-06 | api-route-serve | The frontend reported a 405 even though the GET route existed because the supervised dev launcher did not reload Uvicorn after backend edits. | Enable Uvicorn reload in `bin/dev` and keep a curl-level route smoke check for frontend API additions. | applied |
| 2026-09-06 | favicon-404 | The browser requested a conventional favicon path that the SPA neither linked nor served. | Add an explicit local favicon asset/link whenever a frontend shell is introduced, and smoke-test the static asset URL. | applied |
| 2026-09-07 | clean-review-issues-reporting | Synthesizing policy review issues from low-confidence fields in the product table flooded the UI with dozens of raw confidence factor strings instead of actual review issues. | Reserve the Review issues table column strictly for quotation/line-item review issues; keep field confidence in the Confidence badge and inspector drawer. | applied |
| 2026-09-07 | extraction-mapping-confidence | Combining source recovery and canonical-field assignment in a lowest-field band made an OCR document appear to contain 120 issues. | Expose weighted extraction confidence and separately deduplicated mapping confidence/issues; show each mapping issue only at its affected schema section. | applied |
| 2026-09-07 | single-gemini-key-and-ocr-config | Having multiple Google key variables caused confusion between Gemini and generic Google APIs, and unconfigured OCR env caused runtime errors. | Standardize strictly on GEMINI_API_KEY, hardcode default Modal OCR endpoint URL, and increase upload limit to 15 MB. | applied |
| 2026-09-08 | railway-liteparse-cli | Declaring only the LiteParse Python wrapper allowed local `npx` fallback behavior to hide a required Node CLI that was absent from the Python production image. | Declare cross-runtime subprocess dependencies in the deployable service, lock their versions, carry them into the runtime image, and resolve them without runtime downloads. | applied |
| --- | --- | --- | --- | --- | --- |
| 2026-09-05 | bootstrap-agent-os | Empty repository has no runnable app or selected toolchain. | Add stack bootstrap task; validation scripts report this explicitly. | queued |
| 2026-09-06 | plan-document-intelligence | Infrastructure-first sequencing delayed the product’s schema-memory differentiator. | Require plans to demonstrate the riskiest product thesis in the earliest viable vertical slice. | adopted |
| 2026-09-06 | plan-document-intelligence | A prior OCR service's convenience HTTP response lost confidence and geometry needed for review evidence. | Treat provider services as adapters behind an explicit, versioned evidence contract. | adopted |
| 2026-09-06 | di-01-schema-learning-json | Curl-level success missed a browser origin mismatch and repeat-upload receipt collision. | Treat a real browser journey and repeated submission as required integration checks for every intake slice. | adopted |
| 2026-09-06 | di-02-commercial-review | A reviewer caught duplicate rules, permissive legacy baselining, and controls separated from their table context. | Require rule ownership, exact legacy signatures, and a browser layout check during review-slice wrap-up. | adopted |
| 2026-09-06 | di-03-durable-learning-sse | A retry queue can empty while its application record still looks pending, which hides exhausted failures. | Require every background task to persist an explicit terminal state and test it separately from queue delivery. | adopted |
| 2026-09-06 | di-08-batch-processing | In-memory or incremental batch counters drift on partial failure or worker restart. | Derive batch aggregate progress directly from database child document state with per-document failure isolation. | adopted |
| 2026-09-06 | di-10-langchain-gemini | Conflating decoupling with omitting LLM reasoning caused a pipeline gap. | Preserve the deterministic-first architecture (fast path, PII scrub, schema cache) while embedding LangChain + Gemini across all extraction sources with offline fallback. | adopted |
| 2026-09-06 | di-06-modal-ocr | Starting a worker before migrations lets stale queued work fail against a partial SQLite schema. | Run migrations before launching Huey and make historical interrupted migrations repairable when their prerequisite table is absent. | adopted |
| 2026-09-06 | di-11-dosage-form-normalization | A field can be semantically correct yet have the wrong ownership boundary for downstream users. | Confirm whether a retained qualifier belongs to product identity, packaging, or commercial terms before finalizing the canonical contract and golden cases. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | A fixed corpus count makes evaluation summaries silently inaccurate when cases are added or removed. | Derive run summaries from the selected persisted cases and test a corpus size that differs from the current default. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | Loading local environment settings accidentally switched a regression run to the live provider. | Backend evaluation commands must disable provider credentials by default and require an explicit live-mode flag. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | Fixture paths rooted at the repository made evaluation assets hard to move and inspect as one package. | Resolve fixture paths relative to the golden dataset and keep evaluation inputs, outputs, mappings, and degraded OCR images together under `backend/evals/`. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | A degraded image cannot fairly be scored as a complete quotation, but it can still verify useful OCR evidence. | Record evidence-level anchor thresholds and an explicit partial-document boundary separately from canonical extraction fidelity. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | Email model context contained personal greetings and signatures despite contact-pattern redaction. | Strip deterministic email greeting/signature blocks before model context while preserving postscript corrections and commercial content. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | A model normalized a source-specific price UOM and omitted a supplier legal suffix, causing a real golden-email failure. | Retain only an explicit legal entity from a signature and reconcile an explicit source UOM only when its item and amount agree; keep the live fixture as a regression gate. | adopted |
| 2026-09-06 | di-12-live-evaluation-runner | Treating a canonical `pack_price` as permission to label every price `pack` erases source commercial meaning. | Keep price UOM as source evidence, model packaging independently, and let semantic reasoning classify unfamiliar supplier price fields; never insert a global UOM fallback. | adopted |
| 2026-09-06 | di-14-prd-v4-ingestion | A temporary layout-to-Markdown experiment drifted from the updated PRD, which explicitly says LiteParse's representation is the source evidence and rejects universal table reconstruction. | Re-read the current PRD before parser changes; keep raw parser output and improve the semantic boundary instead of adding supplier-shaped deterministic table logic. | adopted |
| 2026-09-06 | di-14-prd-v4-ingestion | Raw LiteParse context materially reduced live PDF errors, while generic packaging phrases still needed explicit semantic guidance. | Measure unstructured extraction against the golden cases after each prompt/contract change; keep parser fidelity and model semantic gaps separate. | adopted |
| 2026-09-06 | di-14-prd-v4-ingestion | A generic contract normalization plus prompt clarification closed both live PDF golden cases without supplier-specific rules. | Keep source evidence, semantic extraction, and canonical normalization as separate measurable boundaries; promote each new failure to a generic regression case. | adopted |
| 2026-09-07 | home-source-table | A source-level summary was available in the API but the UI still presented each line item as the primary record, making extraction health and activity hard to understand. | Keep source summaries and product details as separate UI levels, and make every background event carry a user-safe phase/message pair that can be replayed in the detail view. | applied |
| 2026-09-07 | ingest-source-action | Reusing navigation styling for an operational upload action made the information hierarchy ambiguous and duplicated the Home label. | Keep one page title and put the primary action in the page context where its outcome is understood. | applied |
| 2026-09-07 | worker-path-consistency | Relative service paths let a manually started API and a root-started Huey worker silently operate on different SQLite and upload files. | Resolve operational paths at the configuration boundary and pass resource directories explicitly to asynchronous workers. | applied |
| 2026-09-07 | review-policy-v6 | A blended confidence percentage obscured why an extraction needed attention and risked treating model certainty as approval. | Persist observable field reliability/reasons, coverage, system routing, and human outcomes as separate concepts; keep numeric evidence provenance out of user-facing decisions. | applied |
| 2026-09-07 | mapping-provenance | Persisting a deterministic JSON path copy as field evidence conflated transformation lineage with proof that a source supports a value. | Reserve evidence records for source locations/excerpts and human actions; store deterministic validation separately and do not present internal provenance in routine product review. | applied |
| 2026-09-07 | confidence-language | “Independent checks” sounded like external verification even though the system compares only compatible values within the supplied source. | Name this reviewer-facing concept “Consistency checks” and state plainly when no comparable figures exist. | applied |
| 2026-09-07 | normalized-unit-prices | A normalized-price field was blank when the source had already stated a price per tablet, creating an apparent missing-value defect. | Treat an explicit quoted amount and UOM as the normalized value when no pack-price conversion applies, while never inventing a UOM. | applied |
| 2026-09-07 | api-background-processing | Huey could lose a claimed job while its document remained `running`, making the local processing state harder to operate than the extraction itself. | Keep the take-home on API-owned background tasks until a durable queue is introduced with timeouts, idempotency, stale-job recovery, and observability. | applied |
| 2026-09-07 | api-lifecycle-terminal-logs | Queue and browser status alone did not show whether an accepted upload had actually begun extraction. | Emit safe API lifecycle records at schedule/start/finish/failure boundaries and keep task tests tied to those observable stages. | applied |
| 2026-09-07 | json-extraction-without-schema-reuse | Treating source-to-canonical interpretation as the extraction result made unfamiliar but correctly recovered JSON facts appear failed or low-confidence. | Persist source-grounded facts independently, validate their JSONPath/value claims, and make canonical normalization an optional downstream projection. | applied |
| 2026-09-07 | extraction-presentation-boundaries | Splitting a complete dosage phrase and routing an empty product result into review made normalization look like successful extraction. | Preserve semantically complete source values and require at least one reviewable product before entering human review; retain partial facts separately for audit. | applied |
| 2026-09-07 | ocr-multimodal-semantic-extraction | Passing OCR coordinates and line objects to a semantic model encouraged the pipeline to treat imperfect OCR layout as authoritative structure. | Keep provider geometry for audit, but give the model readable OCR text as an aid alongside the authoritative source media and explicitly reject line-order row assumptions. | applied |
| 2026-09-07 | peer-image-extractions | Automatically preferring a direct-vision extraction over OCR-assisted extraction hides disagreement and turns a model heuristic into a review decision. | Persist both image extraction paths as peers and require an explicit reviewer action before creating the reviewable quotation. | applied |
| 2026-09-07 | product-detail-editability | A single correction dropdown limited reviewers to a small subset of visible product fields, while the API accepted arbitrary canonical paths. | Keep the UI field registry and backend allowlist aligned across every source-backed line-item section; leave only derived values such as normalized price read-only. | applied |
| 2026-09-07 | mapping-residual-score | A clean direct JSON mapping displayed 98% despite zero mapping issues because the policy reserved an unexplained 2% deduction. | Do not manufacture residual uncertainty; exact source-key/canonical matches score 100%, while deductions must come from an observable conflict or weaker provenance. | applied |
| 2026-09-07 | extraction-confidence-explainability | Showing every weighted factor by default made a clean PDF score look arbitrary and forced reviewers to interpret implementation terms such as OCR quality. | Show one plain-language recovery result by default; put the calculation inputs in an accessible tooltip, and deduct only for observed recovery problems. | applied |
| 2026-09-07 | mapping-confidence-explainability | A mapping percentage without its field-level basis looked like another opaque score even when issue counts were shown separately, and hover-only browser titles were not discoverable on touch/click. | Add click-toggle explanations at source, product-row, and product-detail levels explaining the average field score, contributing reasons, and separate issue count. | applied |
| 2026-09-07 | mapping-definition-discoverability | A per-row tooltip did not make the overall mapping-confidence model discoverable in the product table. | Put an explicit click-to-open help control beside the Mapping confidence column heading, with the scoring model stated in plain language. | applied |
| 2026-09-07 | mapping-section-localization | A product could show 82% mapping confidence with zero actionable issue count, leaving the reviewer unable to tell which detail cards carried weaker field evidence. | Surface sub-100 field mappings as amber review notes inside their corresponding product-detail section cards, while preserving the separate actionable issue count. | applied |
| 2026-09-07 | mapping-reason-language | “Source evidence usable; association limited; independent validation unavailable” exposed implementation vocabulary without telling the reviewer what was actually observed. | State the concrete evidence gap in plain language: the value was found, but the source did not explicitly identify it as the field or provide a second value to confirm it. | applied |
| 2026-09-08 | home-csv-export | Source-level summaries alone made it difficult to take the reviewed product data into an external analysis workflow. | Export every source representation as typed CSV rows—source, product, source fact, mapping issue, and review—with repeated source context. | applied |
| 2026-09-08 | image-attempt-confidence | Model-output disagreement was initially treated as extraction quality, but extraction confidence belongs to the condition and legibility of the uploaded source itself. | Expose image attempts separately, score source legibility from OCR quality distribution rather than model output, and distinguish zero mapping issues from a missing assessment. | applied |
| 2026-09-08 | mapping-and-activity-consistency | Medium mapping uncertainty could display beside `No issues found`, and a completed image-review event inherited the default active phase. | Make every sub-100 mapped field a counted issue and let terminal document/event states override historical activity labels. | applied |
| 2026-09-08 | ocr-quality-safety-gate | Treating OCR quality only as a score still allowed semantic models to produce plausible output from mostly illegible pixels. | Use OCR quality both continuously and as a configurable pre-model gate; exclude rejected text and image regions from downstream semantic extraction. | applied |
| 2026-09-08 | ocr-quality-safety-gate | Treating OCR quality only as a score still allowed semantic models to produce plausible records from mostly unreadable pixels. | Use OCR quality both continuously and as a configurable pre-model gate; mask rejected regions so downstream models cannot recover or guess from them. | applied |
## 2026-09-08 — Upload failure observability

- Application-level exception logging at externally exercised boundaries is essential: platform HTTP logs can report a 500 without retaining the Python traceback needed to diagnose it.
